<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta http-equiv="content-type"
        content="text/html; charset=UTF-8">
    <title><%= title %></title>
    <link rel="stylesheet" 
    href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" 
    crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>

<body class="container">
  <header>
    <h1 class="display-4">
      <%= title %></h1>
  </header>
  <div role="main">
    <p class="h5 my-4">Hi,<span><%= login.name %></span>!<br>Welcome to <%=title%>.</p>
    <form method="post" action="/sche">
      <div class="form-group">
          <label for="find">FIND</label>
          <input type="text" name="find" id="find" value="<%= form.find %>" class="form-control">
      </div> 
      <input type="submit" value="検索" 
          class="btn btn-primary">
    </form>
    <p class="my-4 h5"><%= message %></p>
    <table class="table">
      <tr>
          <th>開始時間</th><th>終了時間</th><th>場所</th><th>内容</th>
      </tr>
      <% for(let i in content) { %>
        <% var ob = content[i]%>
      <tr>
          <td><%=ob.begin %></td>
          <td><%=ob.end %></td>
          <td><%=ob.place %></td>
          <td><%=ob.content %></td>
      </tr>
      <% } %>
    </table>
    <p> </p>
    <p><a href="/sche/add">※データを登録</a></p>
  </div>
</body>

</html>